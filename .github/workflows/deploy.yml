name: Deploy to punhub.com

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /home/customer/www/punhub.com/public_html
            git update-index --no-skip-worktree queue.json 2>/dev/null || true
            git checkout -- queue.json 2>/dev/null || true
            git pull origin main
            git update-index --skip-worktree puns.json queue.json 2>/dev/null || true
